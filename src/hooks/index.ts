/**
 * ============================================================================
 * HOOKS INDEX
 * ============================================================================
 *
 * Export all custom hooks from a single entry point.
 *
 * @version 1.0.0
 */

export { 
  useIntersectionObserver, 
  useIntersectionObserverMany 
} from './useIntersectionObserver';
